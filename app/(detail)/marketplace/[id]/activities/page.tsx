"use client";

interface ActivitiesPageProps {
  params: Promise<{ id: string }>;
}

export default function ActivitiesPage({ params }: ActivitiesPageProps) {
  return <div>Activities page - TODO: Create activities edit component</div>;
}
